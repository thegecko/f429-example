{
    "configurations": [
        {
            "name": "Embedded Debug",
            "type": "embedded-debug",
            "request": "launch",
            "serialNumber": "${command:device-manager.getSerialNumber}",
            "cmsisPack": "${command:device-manager.getDevicePack}",
            "program": "${workspaceFolder}/mbed-os-example-blinky.NUCLEO_F429ZI.elf",
            "debugFrom": "main",
            "pathMapping": {
                "/src": "${workspaceFolder}"
            }
        }
    ]
}